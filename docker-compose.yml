services:
  swagger-test:
    image: byjg/php:8.4-fpm-nginx
    working_dir: /srv
    ports:
      - "8090:80"
    environment:
      - SPEC=swagger
      - PHP_CONTROLLER=/public/app-dist.php
      - NGINX_ROOT=/srv
    volumes:
      - ${PWD}:/srv

